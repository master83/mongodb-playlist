const mocha = require('mocha');
const assert = require('assert');

// describe tests
describe('some test', () => {

    // create tests
    it('adds two numbers', () => {
        assert(2 + 4 === 6);
    });

});